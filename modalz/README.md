# IZ Modal

```
npm install iztools
```

```js
import ModalZ from "iztools/modalz";
// Khởi tạo modal
const modal = new ModalZ();
// Thêm nội dung
const obj = {
  button: "#modal-button",
  content: contentEle,
};

modal.addContent(obj);
```
